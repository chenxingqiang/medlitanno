# 环境变量配置示例
# Environment Variables Configuration Example

# DeepSeek API密钥
# DeepSeek API Key
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Qianwen API密钥
# Qianwen API Key
QIANWEN_API_KEY=your_qianwen_api_key_here

# PubMed 配置
# PubMed Configuration
# 工具名称 (用于API标识)
# Tool name (for API identification)
PUBMED_TOOL=medlitanno

# 邮箱地址 (PubMed要求提供有效邮箱)
# Email address (required by PubMed for API access)
PUBMED_EMAIL=your_email@example.com

# 可选配置
# Optional Configuration

# 数据目录 (默认: datatrain)
# Data directory (default: datatrain)
# DATA_DIR=datatrain

# 输出目录 (默认: annotated_results)
# Output directory (default: annotated_results)
# OUTPUT_DIR=annotated_results

# 最大重试次数 (默认: 5)
# Maximum retry attempts (default: 5)
# MAX_RETRIES=5

# 重试延迟时间/秒 (默认: 10)
# Retry delay in seconds (default: 10)
# RETRY_DELAY=10